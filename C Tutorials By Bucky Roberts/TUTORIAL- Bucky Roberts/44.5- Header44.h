struct EMPLOYEES{
	int ID;
	char FNAME[20];
	char LNAME[20];
	int AGE; 		//I GUESS YOU CAN'T INITIALIAZE AGE = 20 in struct, for now (and maybe forever), just dont initialize values in structure, in structure just declare variables.
	int WEIGHT;
};

//AND IF YOU CANT COMPILE HEADER FILE PROPERLY, JUST TRY TO ONLY COMPILE IT AND RUN THE MAIN PROGRAM first
